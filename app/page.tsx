'use client'; import { useAuth } from '@/context/AuthContext'; export default function Home(){ const { user, loading, signInWithGoogle, logout } = useAuth(); if(loading) return <div style={{padding:40}}>Loading...</div>; if(user===null) return <div style={{padding:40}}><button onClick={signInWithGoogle}>Login with Google</button></div>; return <div style={{padding:40}}><h2>Welcome {user.email}</h2><button onClick={logout}>Logout</button></div>; }
