'use client'; import { useAuth } from '@/context/AuthContext'; import { useRouter } from 'next/navigation'; import { useEffect } from 'react'; export default function Home(){ const { user, loading, signInWithGoogle } = useAuth(); const router = useRouter(); useEffect(()=>{ if(user){ router.push('/routes'); } },[user]); if(loading) return <div style={{padding:40}}>Loading...</div>; if(user===null) return <div style={{padding:40}}><button onClick={signInWithGoogle}>Login with Google</button></div>; return <div style={{padding:40}}>Redirecting...</div>; }
